{
  "name": "qpoll-root",
  "version": "1.0.0",
  "description": "Root package for qpoll project",
  "private": true,
  "scripts": {
    "dev": "concurrently 'npm run dev -w api' 'npm run dev -w ui'",
    "start:api": "npm run dev -w api",
    "start:ui": "npm run dev -w ui"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "api",
    "ui"
  ]
}
